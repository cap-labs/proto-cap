syntax = "proto3";

package cap;

message StreamPacket {
  string PeerId = 1;
  bytes Content = 2;
}

service NetworkStream {
  rpc Handle(stream StreamPacket) returns (stream StreamPacket);
};

service Store {};

service Application {};